<?php

// var_dump($result);
if ($result->count() > 0) {
?>
    <table class="table table-bordered table-hover">
        <thead class="thead-light">
            
                <br>
                <!-- <td colspan="12">Results quantity: <?php echo $result->count(); ?></td> -->
                
                <?php if($this->session->get('AUTH_ROLE')=='guru'){
                    echo $this->tag->linkTo(["/kelas/addClass", "Buat Kelas Baru", 'class' => 'btn btn-success']);
                } ?>
                <br>
                <br>
            <tr>
                <th>Mata Pelajaran</th>

                <th>Pengajar</th>
                <th>Ruang</th>
                <th>Waktu</th>
                <th></th>

            </tr>
        </thead>
        <tfoot>
            <tr>
                <td colspan="13">Results quantity: <?php echo $result->count(); ?></td>
            </tr>
        </tfoot>
        <tbody>
            <?php foreach ($result as $data) { ?>
                <tr>
                    <td><?php echo $data->mapel; ?></td>

                    <td><?php echo $data->pengajar; ?></td>
                    <td><?php echo $data->ruang; ?></td>
                    <td><?php echo $data->waktu; ?></td>
                    <td><?php if($this->session->get('AUTH_ROLE')=='guru'){
                        echo $this->tag->linkTo(["kelas/edit/$data->kelas_id", '<i class="fa fa-sign-out" aria-hidden="true"></i> Edit Kelas', "class" => "btn btn-primary"]); 
                    }; ?>
                    <?= $this->tag->linkTo(["user/unenroll/$data->kelas_id", '<i class="fa fa-sign-out" aria-hidden="true"></i> Batal Ambil', "class" => "btn btn-danger"]); ?></td>

                </tr>
            <?php } ?>
        </tbody>
    </table>
    <?php echo $this->tag->linkTo(["/user/profile", "Back", 'class' => 'btn btn-danger']); ?>
<?php
}
